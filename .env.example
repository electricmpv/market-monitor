# ============================================================================
# AI Market Hunter v3.0 - Environment Variables
# ============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (Required)
# -----------------------------------------------------------------------------
# MySQL/TiDB connection string
# Format: mysql://username:password@host:port/database
DATABASE_URL=mysql://user:password@localhost:3306/market_monitor

# -----------------------------------------------------------------------------
# Security (Required)
# -----------------------------------------------------------------------------
# JWT Secret for authentication (随机生成32字符以上字符串)
# Generate: openssl rand -base64 32
JWT_SECRET=your_random_jwt_secret_min_32_chars_here

# -----------------------------------------------------------------------------
# Push Notifications (Optional)
# -----------------------------------------------------------------------------
# PushPlus Token for WeChat push notifications
# Get yours at: https://www.pushplus.plus
PUSHPLUS_TOKEN=

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Server Port (默认 3000)
PORT=3000

# Timezone (默认 America/Los_Angeles)
TZ=America/Los_Angeles

# Node Environment (production or development)
NODE_ENV=production

# -----------------------------------------------------------------------------
# LLM API Keys (Optional - Configure in Frontend Settings)
# -----------------------------------------------------------------------------
# Note: LLM API Keys are configured in the frontend Settings page
# You can also set them here as fallback, but frontend config takes priority
#
# Search Engine: 用于快速过滤和语义分析 (推荐 Gemini Flash / DeepSeek)
# GEMINI_API_KEY_SEARCH=your_gemini_api_key_for_search
#
# Report Engine: 用于深度分析和报告生成 (推荐 Gemini Pro / Claude)
# GEMINI_API_KEY_REPORT=your_gemini_api_key_for_report

# -----------------------------------------------------------------------------
# Manus Platform (Only if deploying within Manus)
# Leave empty for standalone deployment
# -----------------------------------------------------------------------------
# VITE_APP_ID=
# OAUTH_SERVER_URL=
# VITE_OAUTH_PORTAL_URL=
# OWNER_OPEN_ID=
# OWNER_NAME=
# BUILT_IN_FORGE_API_URL=
# BUILT_IN_FORGE_API_KEY=
# VITE_FRONTEND_FORGE_API_KEY=
# VITE_FRONTEND_FORGE_API_URL=
